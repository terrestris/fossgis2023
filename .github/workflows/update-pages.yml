name: update-pages
on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  update-pages:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
      - run: npm i
      - run: npm run publish-pages
